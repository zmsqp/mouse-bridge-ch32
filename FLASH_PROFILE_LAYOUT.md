# CH32V203C8T 六枪配置 Flash 规划

固件发布版本：`V0.99`。本版本的发布备注见 `V0.99_RELEASE_NOTES.md`。

## 总体分区

芯片内部 Flash 共 64KB，地址范围为 `0x08000000`–`0x0800FFFF`，普通擦除粒度为 4KB。

| 区域 | 物理扇区 | 起始地址 | 结束地址 | 大小 | 用途 |
|---|---:|---:|---:|---:|---|
| 固件程序 | 0–14 | `0x08000000` | `0x0800EFFF` | 60KB | 启动代码、USB Host/Device、压枪逻辑 |
| 六枪配置库 | 15 | `0x0800F000` | `0x0800FFFF` | 4KB | 清单、六个固定槽、预留空间 |

链接脚本已把程序 Flash 长度限制为 60KB。以后即使程序继续增大，链接器也不会把代码或只读数据放进配置扇区。

## 配置扇区内部布局

Flash 的硬件擦除单位仍是整个 4KB 扇区；下表中的 512B 是便于协议和后期开发使用的固定逻辑槽位。六套配置在 RAM 中收齐并校验后统一擦写一次，不能逐槽擦除。

| 逻辑槽 | 枪械 | 起始地址 | 分配大小 | 当前记录大小 | 切换操作 |
|---:|---|---:|---:|---:|---|
| 清单 | — | `0x0800F000` | 512B | 32B | 记录版本、有效位图、默认枪、CRC、写入代次 |
| 0 | AK | `0x0800F200` | 512B | 208B | 滚轮按下 + 前侧键 |
| 1 | 幻影 | `0x0800F400` | 512B | 208B | 滚轮按下 + 后侧键 |
| 2 | 战神 | `0x0800F600` | 512B | 208B | 滚轮按下 + 鼠标左键 |
| 3 | 奥丁 | `0x0800F800` | 512B | 208B | 滚轮按下 + 鼠标右键 |
| 4 | 骇灵 | `0x0800FA00` | 512B | 208B | 滚轮按下 + 向前滚一点 |
| 5 | 獠犬 | `0x0800FC00` | 512B | 208B | 滚轮按下 + 向后滚一点 |
| 预留 | — | `0x0800FE00` | 512B | — | 后续日志、双备份或格式升级 |

每套枪械记录最多保存 30 个轨迹阶段。记录和清单分别带 CRC16；写入时先写六个记录，最后写清单，因此半途断电形成的残缺数据不会被当作有效配置加载。成功组合烧录后，普通断电不会丢失配置。

上位机允许每把枪使用不同的 JSON 数量：主轨迹 JSON 必选，细调 JSON 可选。只选择主轨迹时原样转换；同时选择主轨迹和细调时，上位机先沿用外环＋内环算法融合为最多 30 个阶段，再把融合结果写入该枪唯一的 512B 槽位。因此单 JSON 与双 JSON 枪械可以在同一次六枪组合烧录中混用，不额外占用 Flash。

## 运行规则

- 上电默认选择 AK，但压枪准备状态固定为关闭。
- 单独按前侧键只负责开启/关闭压枪准备状态。
- 任意组合键切枪成功后，压枪准备状态立即关闭，必须再单独按一次前侧键才能启用新枪方案。
- 中键和参与切枪的按键/滚轮量会从发往电脑的鼠标报告中屏蔽，防止切枪时误点或误开火。
- 运行中切枪只读 RAM，不重复擦写 Flash，避免消耗 Flash 擦写寿命。

## LED 状态

| LED 表现 | 含义 |
|---|---|
| 熄灭 | 压枪准备关闭（含上电默认状态） |
| 常亮 | 压枪准备已开启，等待左键开火 |
| 快闪 | 左键开火并正在执行压枪轨迹 |
| 慢闪约 1.2 秒 | 切枪成功确认；之后回到熄灭状态 |

## 串口组合烧录协议

上位机按以下事务发送：`BANKBEGIN` 清空 RAM 暂存有效位，随后对每把枪发送原有 `SET`、`ST2` 和 `SLOT 0..5`，最后发送 `BANKSAVE`。固件仅在六个槽全部就绪时接受 `BANKSAVE`，然后擦除并重写扇区 15。`SEL 0..5` 可用于串口调试切枪。
