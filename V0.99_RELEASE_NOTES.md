# V0.99 固件备注

- 新增六把枪的独立压枪方案，并统一固化到内部 Flash，正常断电后配置不会丢失。
- 上电默认选择 AK 且压枪关闭；任意组合键切枪后也会关闭压枪，必须再按一次前侧键开启。
- 前侧键固定用于开启/关闭压枪；切枪组合键按用户约定映射到 AK、幻影、战神、奥丁、骇灵和獠犬。
- 六套配置使用一个 4KB 配置扇区，含清单、CRC、六个固定槽和预留空间；写入时最后提交清单，避免不完整数据被加载。
- LED 状态：熄灭表示关闭，常亮表示已准备，快闪表示正在压枪，慢闪表示切枪成功。

详细地址和后期开发约束见 `FLASH_PROFILE_LAYOUT.md`。
