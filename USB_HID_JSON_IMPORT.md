# 同一鼠标 USB 口的 JSON 配置导入协议

## 结论与接口选择

可行。PC 侧仍连接 PA11/PA12 的同一个 USB 口，设备不会变成虚拟串口。USB 设备现在包含三个 HID 接口：

| 接口 | 用途 | 电脑识别 |
|---:|---|---|
| 0 | 转发物理鼠标的原始输入报告 | 鼠标 |
| 1 | 压枪位移注入 | 鼠标 |
| 2 | 厂商自定义 Feature Report，Usage Page `0xFF00` | SBZFQ 配置通道 |

接口 2 与两个鼠标接口属于同一个 VID/PID `1A86:FE01`、同一根 USB 线。它不占用 PA9 串口，原有串口命令和六枪烧录流程继续保留，出现 USB 驱动或网页权限问题时仍可用串口排查。

采用独立厂商 HID 接口，而不把配置字段附加到鼠标报告描述符。原因是接口 0 的报告描述符会根据接入的物理鼠标动态克隆；独立接口不会改变鼠标报告长度、按键布局和转发时序，同时可被 Qt 的 hidapi 和浏览器 WebHID 访问。

## “JSON 导入模式”的含义

Qt 或网页先读取并校验 JSON，再把它转换成 MCU 可直接使用的紧凑轨迹结构。不会把带字段名、空格和任意扩展内容的原始 JSON 文本交给 MCU 解析，这样可减少 RAM 占用，并避免不同 JSON 写法造成固件解析差异。

主机首先发送固定起始数据 `JSONBANK`。CH32 校验魔数、协议版本、会话号和 CRC 后才进入 JSON 导入模式。未收到该起始包时，普通 Feature Report 不会修改六枪配置。

## Feature Report

- HID Report ID：`0`（无显式 Report ID）。
- USB 有效载荷：63 字节。
- hidapi 缓冲区：前面加一个值为 `0` 的 Report ID 占位字节，共 64 字节。
- WebHID 的 `sendFeatureReport(0, data)` 与 `receiveFeatureReport(0)` 直接使用 63 字节有效载荷。
- 每包 CRC：CRC-16/CCITT-FALSE，多项式 `0x1021`，初值 `0xFFFF`。

### 主机到设备的 63 字节格式

| 偏移 | 大小 | 内容 |
|---:|---:|---|
| 0 | 4 | 固定魔数 ASCII `SBU1` |
| 4 | 1 | 命令 |
| 5 | 1 | 非零会话号 |
| 6 | 1 | 顺序号 |
| 7 | 1 | 枪械槽位 0–5；非分包命令填 `0xFF` |
| 8 | 2 | 当前分包在枪械数据中的偏移，小端 |
| 10 | 2 | 本枪总数据长度，小端 |
| 12 | 1 | 本包数据长度，最大 48 |
| 13 | 48 | 数据，不足补零 |
| 61 | 2 | 前 61 字节 CRC，小端 |

### 设备到主机的主要字段

响应同样以 `SBU1` 开头并带 CRC。偏移 4–9 依次为：回应命令、会话号、回应顺序号、状态码、设备期待的下一顺序号、已完整收到的枪械位图。偏移 10 表示是否处于导入模式，11 为协议版本，12–13 为当前枪械和 Flash 有效位图。其余字段返回当前启用状态、补偿参数、DPI 和灵敏度，供 Qt 刷新界面。

## 六枪事务传输流程

1. `PING (0x01)`：确认连接到接口 2，而不是鼠标接口。
2. `BEGIN (0x10)`：顺序号为 0，数据为 `JSONBANK + 0x01 + 0x06`。成功状态为 `READY (0x01)`。
3. `DATA (0x11)`：从顺序号 1 开始，单包最多 48 字节。每把枪完成后设备返回 `PROFILE_OK (0x02)` 并更新位图。
4. `COMMIT (0x12)`：六位有效位图必须为 `0x3F`，数据为 `FLASHNOW`。设备校验全部配置后统一擦写配置扇区。
5. 只有收到 `FLASH_OK (0x03)` 且 Flash 有效位图为 `0x3F`，上位机才可显示“烧录成功、断电保存”。

导入会话 5 秒无有效数据会自动取消。每个通信包和每套枪械配置分别带 CRC；顺序号、偏移或会话号不一致时拒绝继续。六套配置先暂存在独立 RAM 区域，传输失败不会替换当前正在运行的配置。

## 每把枪的紧凑数据

固定头 16 字节依次保存：格式版本、启用状态、固定标志、阶段数、启动延迟、游戏 DPI/灵敏度、标定 DPI/灵敏度、标定 Y。随后是 1–30 个阶段，每阶段 6 字节：持续时间、X×100、Y×100。末尾再附加本枪数据 CRC16。

最大一套配置 198 字节，需要 5 个 Feature Report；六枪最大约 30 个数据包。它不会占用新的 Flash 区域，仍写入 `FLASH_PROFILE_LAYOUT.md` 规划的扇区 15。

## 网页使用限制

- WebHID 目前适合 Chromium 内核浏览器，例如 Chrome 或 Edge。
- 页面需要 HTTPS 或 `localhost`，并由用户点击按钮后授权设备。
- 网页筛选 VID/PID `1A86:FE01`，再选择 Usage Page `0xFF00`、Usage `0x0001` 的集合。
- Qt 和网页不要同时占用配置接口；鼠标输入接口不受影响。
- 网页参考传输实现见 Qt 仓库的 `webhid/usb_profile_transport.js`。

## 相关实现文件

- 固件状态机：`User/bridge_usb_import.c/.h`
- USB 复合描述符：`User/USBLIB/CONFIG/usb_desc.c`
- Qt 传输层：`drivers/usb_profile_transport.py`
- Qt 兼容入口：`drivers/driver_hid.py`
