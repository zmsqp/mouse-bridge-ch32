# V1.00 固件备注

V1.00 在用户已验证通过的 V0.99 六枪混合压枪基础上，新增客户可直接使用现有鼠标 USB 线完成的 APP 固件升级。

## 本版新增

- 固定 12KB Bootloader，USB 身份为 `1A86:FE02`。
- 唯一 APP 从 `0x08003100` 启动，中断向量由 RISC-V `mtvec` 自动随链接地址平移。
- APP 通过 FE01 厂商 HID 接收 `ENTER_IAP`，先 ACK，再写 SRAM 邮箱并复位进入 Bootloader。
- Bootloader 逐扇区擦除 APP、按 44B 分片写入并回读、增量 CRC32 校验，最后才写 VALID 标记。
- 擦除、下载、校验均向 Qt 返回设备实际进度；失败状态和偏移保存在 APP 镜像头。
- 升级中断电后保持 FE02 恢复模式，可重新完整上传，不需要客户持有串口或调试器。
- 新增工厂合并工具，首次生产可生成 `SBZFQ_FACTORY.hex`。

## 保持不变

- 六枪配置仍固定在 `0x0800F000–0x0800FFFF`，USB APP 升级不会擦除该区域。
- 每把枪可用单 JSON，或按文件名前缀数字自动识别粗调/细调双 JSON。
- 切枪后压枪仍强制关闭，必须再单独按一次前侧键开启。

## 构建结果

- Bootloader：`11,700B / 12KB`。
- APP：Flash `38,680B / 48,896B`；RAM `14,344B / 20,464B`。
- 软件构建和静态地址检查已通过；发布给客户前仍需完成硬件正常升级、各阶段拔电恢复和六枪配置保留测试。
